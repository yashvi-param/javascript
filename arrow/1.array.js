// An array in JavaScript is a data structure used to store multiple values in a single variable. It can hold various data types and allows for dynamic resizing. Elements are accessed by their index, starting from 0.

// example

// creating an array using array literals

let fruits = ["apple", "orange", "banana", "strawberry", "guava"];

console.log(typeof fruits); // array it self is type of an object

console.log("fruits Names", fruits);

// creating using array constructor

let numArray = new Array(10, 20, 30, 40);

console.log("numArray", numArray);